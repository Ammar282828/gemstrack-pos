
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Only authenticated Firebase users (signed in via Google) can read/write.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
